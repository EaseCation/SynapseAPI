# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

SynapseAPI是EaseCation分布式架构中的关键通信协议实现，负责Nukkit游戏服务器与Nemisys代理服务器之间的通信。它提供了一套完整的跨服务器通信机制，支持玩家转移、数据同步、消息传递等核心功能。

## 核心架构

### 协议设计
- **自定义协议**: 基于TCP的二进制通信协议
- **双向通信**: 支持服务器间的双向消息传递
- **可靠传输**: 提供消息确认和重传机制
- **压缩支持**: 可选的数据压缩减少网络开销

### 核心组件
- **客户端实现**: Nukkit服务器端的Synapse客户端
- **服务端实现**: Nemisys代理服务器端的Synapse服务端
- **消息系统**: 完整的消息定义和序列化机制
- **连接管理**: 连接池、重连、心跳等连接管理

### 通信功能
- **玩家转移**: 玩家在不同游戏服务器间的无缝切换
- **数据同步**: 跨服务器的游戏数据同步
- **广播消息**: 全服务器的消息广播功能
- **服务发现**: 动态的服务器注册和发现

## 关键功能详解

### 玩家转移机制
- **无缝切换**: 玩家在服务器间切换时保持连接
- **状态保持**: 玩家数据、背包、位置等状态的保持
- **权限同步**: 玩家权限和等级信息的同步
- **快速转移**: 优化的转移速度和用户体验

### 消息传递系统
- **消息类型**: 定义了丰富的消息类型覆盖各种场景
- **序列化**: 高效的消息序列化和反序列化
- **优先级**: 支持消息优先级和紧急消息处理
- **批量处理**: 支持消息的批量发送和处理

### 负载均衡
- **服务器状态**: 实时监控各游戏服务器的负载状态
- **智能分配**: 根据负载情况智能分配玩家
- **动态调整**: 根据实时情况动态调整负载分配策略
- **故障转移**: 服务器故障时的自动故障转移

### 安全机制
- **认证系统**: 服务器间的身份认证和授权
- **数据加密**: 敏感数据的传输加密
- **访问控制**: 细粒度的访问权限控制
- **审计日志**: 完整的通信审计和日志记录

## 关键代码文件

### 核心API类
- `src/main/java/org/itxtech/synapseapi/SynapseAPI.java` - 主API类
- `src/main/java/org/itxtech/synapseapi/SynapseInterface.java` - 接口定义

### 客户端实现
- `src/main/java/org/itxtech/synapseapi/SynapseEntry.java` - 客户端入口
- `src/main/java/org/itxtech/synapseapi/network/SynapseClient.java` - 客户端实现

### 消息系统
- `src/main/java/org/itxtech/synapseapi/network/protocol/` - 协议消息定义
- `src/main/java/org/itxtech/synapseapi/messaging/` - 消息处理系统

### 工具和辅助
- `src/main/java/org/itxtech/synapseapi/utils/` - 工具类集合
- `src/main/java/org/itxtech/synapseapi/event/` - 事件系统

## 开发规范

### 协议开发
- 遵循现有的消息格式和序列化标准
- 确保新消息类型的向后兼容性
- 实现完整的消息处理和错误处理
- 注意消息的性能和网络开销

### 客户端集成
- 正确初始化和配置Synapse客户端
- 处理连接断开和重连逻辑
- 实现消息的异步处理和回调
- 管理连接池和资源释放

### 服务端开发
- 实现高并发的消息处理能力
- 提供负载均衡和故障转移机制
- 监控和统计服务器性能指标
- 实现安全的认证和授权机制

### 性能优化
- 优化消息的序列化和网络传输
- 实现合理的缓存和连接复用
- 控制消息的发送频率和批量处理
- 监控网络延迟和吞吐量指标

## 构建和部署

### 构建命令
```bash
./gradlew :SynapseAPI:shadowJar
```

### 部署配置
- **Nukkit服务器**: 作为插件部署到`{server}/plugins`
- **Nemisys代理**: 集成到Nemisys代理服务器
- **配置文件**: 网络配置、认证配置、负载均衡配置

### 测试环境
- 使用`./gradlew copyShadowJar`部署到本地测试环境
- 代理服务器：`_proxy/`目录
- 游戏服务器：`_server/plugins/`目录

## 常见开发任务

### 添加新消息类型
1. 定义消息的数据结构和字段
2. 实现消息的序列化和反序列化
3. 添加消息的处理逻辑和回调
4. 测试消息的发送和接收

### 优化网络性能
1. 分析网络延迟和吞吐量瓶颈
2. 优化消息的批量处理和压缩
3. 改进连接管理和重连机制
4. 监控网络性能和质量指标

### 实现新功能
1. 设计功能的通信协议和流程
2. 实现客户端和服务端的协调逻辑
3. 处理异常情况和错误恢复
4. 测试功能的稳定性和性能

### 调试通信问题
1. 检查网络连接和协议配置
2. 分析消息的发送和接收日志
3. 验证数据的序列化和完整性
4. 排查性能和延迟问题

## 性能优化要点

- 优化消息序列化的性能和内存使用
- 实现高效的网络I/O和连接管理
- 控制消息队列的大小和处理优先级
- 使用连接池和资源复用减少开销
- 监控和调优网络参数和缓冲区大小